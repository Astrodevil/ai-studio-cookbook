[project]
name = "models"
version = "0.1.0"
description = "Jupyter notebook collection for running model guides and examples"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.48.0",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
